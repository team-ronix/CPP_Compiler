int main () {
    // should pass: combined flow-control coverage (no syntax/semantic errors)
    int a = 0;
    int sum = 0;

    if (a == 0) {
        sum = 1;
    } else if (a > 0) {
        sum = 2;
    } else {
        sum = 3;
    }

    while (a < 3) {
        if (a == 1) {
            a = a + 1;
            continue;
        }
        sum = sum + a;
        a = a + 1;
    }

    do {
        sum = sum + 2;
    } while (sum < 10);

    for (int i = 0; i < 4; i = i + 1) {
        if (i == 2) {
            continue;
        }

        switch (i) {
            case 0:
                sum = sum + 10;
                break;
            case 1:
                sum = sum + 20;
                break;
            default:
                sum = sum + 30;
        }

        if (sum > 80) {
            break;
        }
    }

    int flag = 0;
    switch (sum) {
        case 10:
            flag = 1;
            break;
        case 20:
            flag = 2;
            break;
        default:
            flag = 3;
    }

    bool ok = flag != 0;
    return ok;
}